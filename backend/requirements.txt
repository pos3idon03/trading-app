# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
# LangChain 1.x requires pydantic>=2.7.4 (conflicts with older pins).
pydantic>=2.7.4,<3
pydantic-settings==2.2.1

# Database
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
psycopg2-binary==2.9.9
alembic==1.13.1

# Data science — versions left flexible so pip can resolve vectorbt conflicts
pandas>=2.1.0,<3.0
numpy>=1.26,<3.0
# Lower bound avoids pip backtracking to SciPy 1.11.x source builds (slim image lacks full Fortran toolchain).
scipy>=1.13.0,<2
scikit-learn>=1.4.0,<2

# Market data providers
yfinance>=0.2.40
# Pin avoids resolver picking legacy alpaca-py (<0.43) that caps websockets<12 (conflicts with websockets>=12).
alpaca-py>=0.43.0,<1
requests>=2.31.0
httpx>=0.27.0

# Technical analysis + backtesting
# PyPI currently ships pandas-ta only as pre-releases (0.4.xb); pin explicitly for reproducible installs.
pandas-ta==0.4.71b0
# 0.26.x requires numpy<2; pandas-ta 0.4.x pulls numpy 2–compatible numba — use vectorbt 1.x on Python 3.12.
vectorbt>=1.0.0,<2

# Live streaming (Phase 5)
websockets>=12.0

# Scheduling
apscheduler>=3.10.0

# Logging / tracing
structlog>=24.1.0

# LLM / AI (Phase 4 onwards)
# 0.2.x pulls a legacy stack and triggers extreme resolver backtracking with NumPy 2 / SciPy wheels.
langchain>=1.0.0,<3
litellm>=1.40.0
crewai>=0.80.0
# Vertex AI SDK required by LiteLLM's vertex_ai/ provider for Gemini fallback
google-cloud-aiplatform>=1.50.0

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
