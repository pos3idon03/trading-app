# Optional dependencies for FinBERT news sentiment analysis.
# Install after base requirements: pip install -r requirements.txt -r requirements-sentiment.txt
transformers>=4.40.0,<5.0
# torch: installed in Dockerfile (TORCH_CUDA build arg)
sentencepiece>=0.2.0,<1.0
safetensors>=0.4.0,<1.0
